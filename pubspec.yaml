name: inspired_gospel_hymn
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  google_fonts: ^1.1.1
  palette_generator: ^0.2.3
  page_transition: ^1.1.7+3
  simple_animations: ^1.3.11
  font_awesome_flutter: ^8.11.0
  animations: ^1.1.2
  flutter_launcher_icons:
  share: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/

flutter_icons:
  image_path: 'assets/GIcon.png'
  android: true
  ios: true